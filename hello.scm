
; Print "Hello, World!"
(display "Hello, World!")
(newline)

; Defining primitive types
(define num 42)
(define pi 3.1415)
(define schemeTheGoat #t)
(define name "Prolog")

; Output the values
(display "Integer: ") (display num) (newline)
(display "Float: ") (display pi) (newline)
(display "Boolean: ") (display schemeTheGoat) (newline)
(display "String: ") (display name) (newline)